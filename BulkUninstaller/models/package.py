class Package:
    def __init__(self, name, package_id, version="", source=""):
        self.name = name
        self.package_id = package_id
        self.version = version
        self.source = source
